const initVue = (obj) => {return new Vue(obj);}
const jq = () => {return $;}
const jqSelector = (selector) => {return $(selector);}
const xHttpReq = () => {return new XMLHttpRequest();}
const webSocket = (host) => {return new WebSocket(host);}
const eventSource = (host) => {return new EventSource(host);}
